# Collect all styles 

# Collect all input images

# Collect hyperparameters (color, )

# create powerset

# grid search over and save off in a neat directory structure for easy viewing later
# for each (src_img, style_img, hparam) in powerset(src_imgs, style_imgs, hparams):
#   create_results_dir(src_path, style_path)
#   create_intermediate_images(src_img, style_Img, hparam) 